name: Mergeabot

on:
  pull_request:
  schedule:
    - cron: "0 0 * * *"

jobs:
  mergeabot:
    runs-on: ubuntu-latest
    steps:
      - uses: freckle/mergeabot-action@v1
        with:
          quarantine-days: -1
